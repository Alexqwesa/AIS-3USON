apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: backend
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.ais3uson.network/backend: "true"
  podSelector:
    matchLabels:
      io.ais3uson.network/backend: "true"
